{
  "name": "sb_lab_server",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "deploy": "cd dist && vercel --prod",
    "build": "tsc && cp -R ./db.json ./dist/ && cp -R ./public ./dist/ && cp -R ./.github/distFiles/vercel.json ./dist/ && cp -R ./.vercel ./dist/ && cp -R ./.github/distFiles/package.json ./dist/",
    "server": "node ./dist/index.js",
    "start": "nodemon ./index.ts"
  },
  "keywords": [],
  "author": "Serge",
  "license": "ISC",
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^22.13.9",
    "nodemon": "^3.1.9",
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.16.4"
  }
}